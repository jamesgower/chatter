{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon client/src/server/server.js",
    "test": "mocha client/src/server/**/*.test.js",
    "test-watch": "nodemon --exec \"npm run test\"",
    "client": "npm run start --prefix client",
    "dev": "concurrently \"npm run start\" \"npm run client\"",
    "heroku-postbuild": "npm install --prefix client && npm run build --prefix client"
  },
  "engines": {
    "node": "9.5.0"
  },
  "author": "James Gower",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "concurrently": "^3.5.1",
    "engine.io": "^3.2.0",
    "express": "^4.16.3",
    "moment": "^2.22.1",
    "requirejs": "^2.3.5",
    "socket.io": "^2.1.0"
  },
  "devDependencies": {
    "expect": "^22.4.3",
    "mocha": "^5.1.1",
    "nodemon": "^1.17.3"
  }
}
